<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itheima.manger.mapper.ClazzMapper">
    <insert id="clazzsave">
        insert into clazz
        (name, room, begin_date, end_date, master_id, subject, create_time, update_time)
        values
            (#{name}, #{room}, #{beginDate}, #{endDate}, #{masterId}, #{subject}, now(), now())
    </insert>
    <update id="clazzupdate">
        update clazz set name=#{name},room=#{room},begin_date=#{beginDate},
        end_date=#{endDate},master_id=#{masterId},subject=#{subject},update_time=now()
        where id =#{id}
    </update>
    <delete id="clazzdelete">
        delete from clazz where id=#{id}
    </delete>
    <select id="clazzpage" resultType="com.itheima.manger.entity.Clazz">
        select c.*,d.name as masterName,
        case
        when now() &lt; c.begin_date then '未开班'
        when now() &gt; c.end_date then '已结课'
        else '在读'
        end as status
        from clazz c
        left join emp d on c.master_id = d.id
        <where>
            <!-- 按班级名称模糊查询 -->
            <if test="name != null and name != ''">
                and c.name like concat('%', #{name}, '%')
            </if>
            <!-- 按开课时间范围查询：begin ~ end -->
            <if test="begin != null and end != null">
                and c.begin_date between #{begin} and #{end}
            </if>
        </where>
    </select>
    <select id="clazzselect" resultType="com.itheima.manger.entity.Clazz">
        select id,name,room,begin_date,end_date,master_id,
               subject,create_time,update_time from clazz where id =#{id}
    </select>
    <select id="clazzlist" resultType="com.itheima.manger.entity.Clazz">
        select id,name,room,begin_date,end_date,master_id,
               subject,create_time,update_time from clazz
    </select>
</mapper>